<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ticketingsitedemo.server.mapper.Host_ApplicationMapper">
    <select id="findAll" resultType="ticketingsitedemo.server.domain.Host_Application">
        SELECT * FROM Users
    </select>
</mapper>