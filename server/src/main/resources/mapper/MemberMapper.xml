<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ticketingsitedemo.server.mapper.MemberMapper">

    <select id="findAll" resultType="ticketingsitedemo.server.domain.Member">
        SELECT * FROM member
    </select>

    <select id="findById" parameterType="long" resultType="ticketingsitedemo.server.domain.Member">
        SELECT * FROM member WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="ticketingsitedemo.server.domain.Member" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member(name, email)
        VALUES(#{name}, #{email})
    </insert>
</mapper>